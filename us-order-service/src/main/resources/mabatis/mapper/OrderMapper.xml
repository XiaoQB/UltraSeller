<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order.dao.OrderDao">

    <sql id="order_all_columns">
        id, status, total_price, address, buyer_id, create_time, update_time
    </sql>

    <insert id="createOrder" parameterType="order.entities.dbo.Order">
        insert into `order` (id, status, total_price, address, buyer_id, create_time, update_time)
            value (
                   #{orderId},
                   #{status},
                   #{totalPrice},
                   #{address},
                   #{buyerId},
                   #{createTime},
                   #{updateTime}
            )
    </insert>

    <select id="getOrderById" resultType="order.entities.dbo.Order">
        select
        <include refid="order_all_columns"/>
        from `order`
        where id = #{orderId}
    </select>

    <update id="changeOrder" parameterType="order.entities.dbo.Order">
        update `order`
        set status      = #{status},
            total_price = #{totalPrice},
            address     = #{address},
            buyer_id    = #{buyerId},
            create_time = #{createTime},
            update_time = #{updateTime}
        where id = #{id}
    </update>

    <select id="getOrdersByUser" resultType="order.entities.dbo.Order">
        select
        <include refid="order_all_columns"/>
        from `order`
        <where>
            <if test="userIds != null and userIds.size > 0">
                buyer_id in
                <foreach collection="userIds" item="userId" separator="," open="(" close=")">
                    #{userId}
                </foreach>
            </if>
        </where>
    </select>

</mapper>
