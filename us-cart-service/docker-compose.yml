version: "3"
services:
  us-cart-service:
    restart: always
    build: .  # '点'代表当前目录
    command: bash -c "python manage.py migrate && python manage.py makemigrations && python NacosClient.py"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
  #   depends_on:
  #     - db
  # db:
  #   image: mysql:5.7
  #   volumes:
  #     - "./mysql:/var/lib/mysql"
  #   ports:
  #     - "3306:3306"
  #   restart: always
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=mypassword
  #     - MYSQL_DATABASE=django_app