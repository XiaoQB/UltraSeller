server:
  port: 8001

spring:
  application:
    name: us-user-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER}
      config:
        server-addr: ${NACOS_SERVER}
        file-extension: yml
        group: DEV_GROUP
