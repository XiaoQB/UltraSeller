server:
  port: 8000

spring:
  application:
    name: us-gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER}
      config:
        server-addr: ${NACOS_SERVER}
        file-extension: yml
        group: DEV_GROUP
