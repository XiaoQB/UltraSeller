server:
  port: 8003

spring:
  application:
    name: us-wallet-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER}
      config:
        server-addr: ${NACOS_SERVER}
        file-extension: yml
        group: DEV_GROUP
